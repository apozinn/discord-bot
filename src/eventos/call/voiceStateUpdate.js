const Discord = require('discord.js');

module.exports = {
    name: 'voiceStateUpdate',
    execute: async(newMember, oldMember, bot) => {
        console.log('alguem entrou na call viu')
    }
}