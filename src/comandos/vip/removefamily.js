const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "removefamily",
        aliases: ['removerfamilia'],
        description: "remova a permissão vip de uma familia",
        usage: "d!removefamily"
    },
    run: async (bot, message, args) => {
        
    }
}