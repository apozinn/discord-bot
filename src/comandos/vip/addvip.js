const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "addvip",
        aliases: [''],
        description: "adicione a sua tag vip em um membro",
        usage: "d!addvip {membro}"
    },
    run: async (bot, message, args) => {
        
    }
}