const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "vipfamily",
        aliases: ['vipfamilia'],
        description: "configure a tag vip da sua familia",
        usage: "d!vipfamily"
    },
    run: async (bot, message, args) => {
        
    }
}