const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "setfamily",
        aliases: ['setfamilia'],
        description: "de permissão de vip para uma familia",
        usage: "d!setfamily {membro}"
    },
    run: async (bot, message, args) => {
        
    }
}