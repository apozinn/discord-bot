const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "rvip",
        aliases: [''],
        description: "remova a sua tag vip de um membro",
        usage: "d!rvip {membro}"
    },
    run: async (bot, message, args) => {
        
    }
}