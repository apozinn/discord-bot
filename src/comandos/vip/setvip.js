const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "setvip",
        aliases: ['setarvip'],
        description: "de permissão de vip para um membro",
        usage: "d!setvip {membro}"
    },
    run: async (bot, message, args) => {
        
    }
}