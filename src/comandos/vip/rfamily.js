const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "rfamily",
        aliases: ['rfamilia'],
        description: "remova a tag vip da sua familia de um membro",
        usage: "d!rfamily {membro}"
    },
    run: async (bot, message, args) => {
        
    }
}