const Discord = require('discord.js');
const db = require('discord.js');

module.exports = {
    config: {
        name: "removevip",
        aliases: ['removervip'],
        description: "remova a permssão vip de um membro",
        usage: "d!removevip {membro}"
    },
    run: async (bot, message, args) => {
        
    }
}